dependencies:
  pre:
    - npm install -g yarn
    - yarn
  override:
    - npm run build

deployment:
  staging:
    branch: master
    commands:
        - npm run build
        - git push heroku master --force
